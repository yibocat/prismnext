# Changelog — 0.6.x series

## 0.6.10 — 2026-08-07

### Built-in skills

- **Writing pipeline**: new section skills — **writing-design** (outline gate), **writing-introduction**, **writing-preliminaries**, **writing-methods**, **writing-results**, **writing-conclusion** — each with reference templates, not fixed molds
- **Ideation**: **idea-lab** for open-ended brainstorming; **hypothesis-design** scoped to pre-run convergence (replaces **project-kickoff**)
- **Figures** renamed and split for clearer routing: **figure-matplotlib** (data plots), **figure-tikz** (TikZ/pgfplots), **figure-interaction** (RightArea panel standards), **figure-pipeline** (manuscript wiring)
- **writing-related-work** replaces **related-work-pipeline**; deepened passes on **critical-review**, experiment skills, **ml-research-protocol**, **symbolic-math**, and others
- **`resources/skills/README.md`**: Chinese catalog with links, analysis checklist, and scenario guide
- **skill-creator** expanded with `SKILL.template.md` for multi-file skills

### Agent tools & vision

- **`image-describe`**: agent tool to describe local image files via the configured multimodal helper when the main model cannot see pixels
- Vision fallback and credential env hardening; interaction blocks dedupe more reliably

### TeX & compile

- **Standalone `.tex` compile** for TikZ / `\documentclass{standalone}` figures — in-place PDF beside the source without touching the main manuscript build
- `latex-compile` and bash compile paths updated for standalone graphics islands

### Settings & reliability

- Startup **orphan provider settings prune** — removes stale API keys / model maps for providers no longer in the custom list (fixes ghost re-registration from ≤ v0.6.7 removals)
- Experiment run env and python gate tweaks; PTY output handling polish

### Docs & site

- README (EN/ZH) and download site updated for the expanded skills library and figure skill names

## 0.6.9 — 2026-08-05

### Built-in skills

- Rebuilt the bundled library: dropped 16 placeholder / off-identity cards; added workflow skills that orchestrate PrismNext tool chains (**related-work-pipeline**, **intensive-reading-notes**, **hypothesis-design**, **experiment-design-matrix**, **experiment-to-methods**, **figure-pipeline**, **critical-review**, **rebuttal-letter**, **manuscript-preflight**, **project-kickoff**) plus kept **skill-creator**
- Heavy folder skills with references, templates, and scripts: **statistical-rigor**, **prisma-systematic-review**, **scientific-visualization**, **symbolic-math**, **latex-tikz-graphics**, **ml-research-protocol**, **management-science-empirical**, **interaction-figures**
- `export-bundled-skills.mjs` is the single source for manifest + SKILL.md; README and the download site frame skills as **codified research standards**, not a skill count
- Settings: read-only preview tab for bundled skills not yet installed (`agent:readBundledSkillMd`)

### Agent & prompts

- Prompt audit: literature-search metadata-only wording; expert modules trimmed per manifest; dead `expert.json` / orchestrator sidecars removed; no fictional `manuscript/main.tex` on unconfigured projects
- Deduped module↔tool copies; orchestrator Task discipline lives only in **orchestrator-judgment**; intensive-reading turn block slimmed

### SyncTeX (removed for now)

- Removed half-wired user surface (shortcut, IPC, keymap) until compiler-side SyncTeX is real; engine stub kept for later

### Docs & download site

- README (EN/ZH): preprint cover, capability galleries, themes point to live download-site compare
- Download site: ink hero backdrop, vertical light/dark theme compare, theme-pack picker, backdrop preview, skills positioning copy

## 0.6.8 — 2026-08-04

### Workspace

- **RightArea launcher**: open an empty RightArea to pick a mode from a vertical column of flat cards (icon · label · shortcut); modes come from the registry (same as「+」). Closing the last tab collapses RightArea — does not return to the launcher
- RightArea「+」menu shows split-open keyboard shortcuts (same chords as Cmd+K)
- **Settings** RightArea only shows settings-editor tabs; leave Settings to restore workspace tabs; closing the last settings detail tab collapses the panel (no empty placeholder)

### Chat & drag-and-drop

- Drag files, literature, PDF MinerU blocks, experiments, browser URLs, RightArea tabs, Git changes, and editor selections into **Chat / AiBar** as inline chips; OS file drops still attach as files
- AiBar capsule (maximized editor) accepts the same drops as the split chat panel; Browser reserves space so the webview does not cover the capsule
- Composer drag ghost is compact (not a full page capture); one chip per drop
- Literature drag grip lives on the MD slot so marquee multi-select and drag do not fight; Add to Chat and selection chips track the selection more reliably

### Project rules & brief

- Agent can write **Project rules** via `project-rule-write` (Settings → Rules); suggestions ask before writing
- Project rules are **global only** — removed per-agent Rules pickers
- Research brief lives at project-root **`.brief.md`** (auto-migrates; hidden in Files); Settings preview opens in the editor when needed

### Agent & prompts

- Knowledge modules attach by role; **orchestrator-judgment** on the primary orchestrator only; no per-agent module / MCP / Skills pickers in the agent editor (MCP and skills are session / project defaults)
- Deeper modules for literature, LaTeX workspace, project brief vs research design, experiments, interaction, scholarly reasoning, reply depth, subagent role, and core persona; thicker bundled orchestrator / expert instructions with a live expert roster
- Settings shows **token estimates** for prompt stack, modules, instructions, and Skills; context ring uses OpenCode-reported usage only
- Multimodal vision helper: reliable send path for text-only mains; wait copy follows 规划 → 识图 → 等待模型

## 0.6.7 — 2026-08-03

### Workspace

- RightArea rebuilt around **tabs +「+」** (Cursor-style): mode icon row removed; close a tab to leave a mode (no forced home reset); singleton modes hide from「+」when open; Terminal / Browser / Literature stay listed to spawn new tabs
- Files / Experiments / Literature: opening detail **replaces** the home tab in place; further opens add tabs; Literature「+」always spawns another Library home
- Mode open state is **derived from tabs only** (writable `activeModes` / `focusedMode` removed); `openMode` / `closeModeTabs` orchestration; Settings RightArea hides「+」and maximize (collapse kept)
- Fix chat / deep-link opens: expanding RightArea works again when the store flag was set before the panel resized

### TeX workspace

- Bundled Tectonic (like OpenCode) with a warm daemon for live compile; dirty-only flush, faster debounce; TeX Live only as silent fallback when Tectonic is missing
- PDF preview: A/B swap without jump-to-page-1; wait for Lector virtualizer before restore; live compile always publishes each successful PDF

### Literature

- Built-in `literature-discover` (arXiv / Crossref / OpenAlex / Semantic Scholar / PubMed); Paper Search MCP removed from product
- Session citation add: select-N add, cancel mid-download without leftover library rows; optional S2 / PubMed API keys in Settings

### Chat

- Click user bubbles to edit in place and resend after **world rollback**; clearer rollback / undo-rollback; composer send queue while streaming (Stop & Send)
- Inline tokens and markdown chips open files / papers / experiments; shared thin-border frames for intensive-reading figures and other chat images
- Context ring uses OpenCode `usage_update`; activity folds, Task plan drawer, background Task; Cmd+K and homepage composer polish

### Theme & Appearance

- Softer shell / border tokens; system-installed fonts; chat backdrops (starfield, bookshelf, paper planes, …) with ⌥T / ⌥B cycle shortcuts

### Models, permissions & reliability

- OpenCode **1.18.10**; lazy provider catalogs; Permissions settings (paths, rules, tester); faster cold start and Stop / typing fixes

## 0.6.5 — 2026-07-28

### Experiments

- Experiments workbench: Files-style sidebar list, multi-tab detail, and Overview / Execution / Results
- Create and manage experiments from the panel (active/archived); link research briefs and tags
- Execution: run history with live output, cancel, compare selected runs, and “Use in paper”
- Results: recorded artifacts by run (with figure previews) plus a working-directory scan view; clearer ownership when paths are shared
- Chat: live command/output while `experiment-run` is in progress; clearer run cards

### Python environment

- One shared project venv at `.prismnext/.venv` (lazy on first need) — no per-island venvs

### Layout

- Fix RightArea open/restore on medium windows so Chat keeps its minimum width

### Chat

- `question` tool: discrete choices must use `options` so multiple-choice prompts show clickable choices

### Interaction (early shell)

- Chat ` ```interaction ` fences open a RightArea panel shell backed by `.prismnext/artifacts/<id>/spec.json`
- Agent tools: `interaction-list` / `read` / `write` / `open` (renderers beyond the shell remain experimental on a separate branch)

## 0.6.4 — 2026-07-26

### Chat

- Rename a session from the session hover card; the title sticks (OpenCode auto-titles no longer overwrite user renames), with undo support

### Browser

- Settings → Browser: choose the in-app search engine (DuckDuckGo, Google, Brave, Bing, Ecosia)

### Shortcuts

- Cycle chat message / composer width with ⌘L / Ctrl+L (was ⌘⇧W)
- Right Area **Add to Chat** from editor, git diff, and terminal selection uses ⌥L / Alt+L (frees ⌘L / Ctrl+L for width); Alt+L is captured so terminals on Windows/Linux do not swallow it

### i18n

- Right Area toolbars and menus (Git / Terminal / TeX / Literature / Browser / Add to Chat / PDF preview) use locale strings instead of hardcoded English
- Right Area empty states (no file / no manuscript / browser home / git history / literature BBT banner) use locale strings

## 0.6.3 — 2026-07-24

### Chat

- Turn rail on the message list right edge: scrub turns, hover for preview (text / time / model), click to jump — also expands the virtual turn window when needed
- At least two turns required before the rail appears

### Command palette

- Lighter backdrop scrim and slightly translucent blurred panel; list text scales with Appearance UI font size
- **All** tab previews: 6 recent sessions / 5 recent files / 5 settings — each with **More** that switches palette tab (not leaving the palette)
- Appearance section in **All**: theme packs, chat backdrops (cycle shortcuts on headings), and UI language — separate from Settings
- Files: hide paths matched by `.gitignore` (`git check-ignore`)
- Sessions: open tabs marked with a small dot; sorted by last activity time on the right; **Sessions** tab lists all sessions (no 20-item cap); titles match left sidebar enrichment
- Literature: left **MD** extract badge matches the Library list (replaces sparkle icon)
- Full command palette: categories (All / Files / Sessions / History / Literature / Modes / Settings), Tab to switch, fuzzy match across names and paths
- Empty All view shows recent files and sessions; successful searches keep a per-project history
- Open files, chat sessions, workspace modes, settings pages, and literature (list + FTS); Shift+Enter opens the target with Right Area maximized
- Quick create: new file, new chat session, new experiment island; open a URL in the in-app browser

### Repo

- Public README media lives under `assets/`; internal design/plan docs moved to gitignored `docs-private/`; `docs/` reserved for future public product documentation

## 0.6.2 — 2026-07-23

### Appearance

- Theme packs (Academic, Midnight, Forest, Warm Paper, Graphite) replace single accent colors — each pack is one hand-tuned palette
- Quiet shadcn surface hierarchy (canvas / card lift / muted PDF well); opaque fills only — no translucent theme tints
- Default corner radius matches shadcn (`0.625rem`); Appearance adds a Default radius option
- App status uses semantic tokens; Git added/deleted follow green/red-family success/destructive across packs, modified/renamed keep conventional amber/violet
- AiBar capsule border follows the active theme border token

### Chat

- Message width tiers: Narrow / Balanced / Wide (Appearance → Chat Layout); cycle with ⌘⇧W / Ctrl+Shift+W
- Composer and AiBar capsule track the active width tier and UI font size

### Workspace & Files

- Fix: switching projects could wipe workspace folders to empty via autosave cleanup — snapshot flush plus server-side empty-list guard so folders recover
- Workspace settings: clearer This project / New project template tabs
- Folder icons recognize common names (`figures`, `src`, `notes`, …) instead of only workspace function

### Fixes

- Packaged app: CSP `wasm-unsafe-eval` so Shiki chat code highlighting works in release builds
- Literature notes: rewrite extract `images/…` paths on Insert into note; markdown preview loads local images in notes

## 0.6.1 — 2026-07-23

### Fixes

- Fix startup race: concurrent ACP `initialize()` from app warm-up and project `chat:prewarm` no longer kills the handshake mid-flight (`ACP connection closed` / Agent project warm-up failed toast)

## 0.6.0 — 2026-07-23

### Shell & Agent

- Industry-standard warm start: splash stays until ACP + project config are ready; last project warms as soon as ACP is up
- Sessions are created only on first send / New Chat — no idle empty-session mint cycle
- Agent status dot is truthful (connected / starting / failed + Retry); project prewarm shown when a project is open
- Optional background update download (About toggle); clearer ready-to-install vs downloading states

### Chat UX

- Turn footer: relative time, model, Restore (when files changed), Copy — persisted per session
- Session hover card: title, checkout, Build/Plan, staged citations, intensive count
- Plan mode toggle ⌥P / Alt+P; homepage Plan suggest shows the shortcut chord
- Chat typography collapsed to body (14) + chrome (13); composer / branch / Plan toolbar density aligned
- Streaming stop uses theme primary (same as send)

### Git & Files

- Git Changes filter persists across mode switches and restarts
- Deleted files show a **Deleted** label
- Image viewer: zoom, pan, pinch / soft ⌘-scroll

### Download & Platforms

- Download site redesign: instrument grid, EN/ZH product screenshots, slogan for co-driven gated research loop
- Linux AppImage in release CI; `linuxUrl` in updater / site metadata
- README (EN/ZH): Early Access tone, badges, platform list including Linux
