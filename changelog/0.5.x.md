# Changelog — 0.5.x series

## 0.5.17 (Unreleased)

### Docs & licensing

- Add Apache `NOTICE` and `package.json` `author` (provisional copyright: yibocat); document future open-source copyright review in CONTRIBUTING

### CI

- Release workflow: bump Actions off deprecated Node 20 runtime (`checkout`/`setup-node`/`upload-artifact`/`download-artifact`/`pnpm/action-setup`)

### Distribution

- Fix Windows `icon.ico`: restore multi-size ICO including 256×256 (broken 16-only file made electron-builder fail)

## 0.5.16 — 2026-07-22

### Updates UX

- One-click update in About, Welcome, and the left sidebar when a newer build is available (download then restart)
- About no longer shows Update source; stale local `updateSource` overrides are ignored so packaged apps use the baked R2 feed
- Startup splash shows the PrismNext ribbon; Dock / installer icons use a larger mark

### Docs & open source

- Apache-2.0 `LICENSE` plus GitHub community files (contributing, security, conduct, Issue/PR templates)
- Product README (EN/ZH) rewritten for Early Access positioning
- Download site (`website/`): branded hero, EN/ZH, OS-aware macOS / Windows CTAs

## 0.5.15 — 2026-07-22

### Distribution & updates

- Private GitHub Actions → Cloudflare R2 installers; GitHub Release notes pulled from `changelog/{major}.{minor}.x.md`
- In-app updates via electron-updater (generic → R2); macOS ships `.dmg` (download) + `.zip` (updater)
- Download landing page under `website/` (Cloudflare Pages)
- Packaged app display name is **PrismNext** (`PrismNext.app` / installer filenames)

### Updates

- electron-updater wired for packaged builds; About supports download progress and restart-to-install
- Default update feed baked from `PRISM_UPDATER_BASE_URL` / R2 public URL at build time



## 0.5.14 — 2026-07-21



### AI Chat — Plan Workflow

- **Build | Plan** session identity with OpenCode `plan` agent permissions (edits limited to research plan paths; bash follows Permission Mode)
- Enter Plan via `/` → Modes → **Plan**, Plan chip, homepage “Plan a new idea”; dedicated **Plan tab** for draft files
- **Plan of record:** per-session draft at `.prismnext/research/plans/drafts/<sessionId>.md` → Approve renames to a dated plan; Deny deletes the draft (`brief.md` stays the living design)
- Draft structure **Analysis → Plan → Checklist**; Approve seeds todowrite and dual-marks progress; missing draft write is hard-enforced (kick + redirect)
- **Suggest Enter Plan:** agent calls `suggest-plan` (research multi-step / multi-phase, including design) → 15s consent strip (timeout ≡ stay Build); no App keyword heuristic; accept returns `draftPath` for same-turn write
- Plan mode blocks Expert Task; fixed orchestrator Task allowlist YAML (`"*": deny`) so expert Tasks work again in Build
- Session reopen keeps Approve/Deny and post-Approve turns; Created Plan card under write/edit tools



### Prompt architecture (Hard / Soft)

- Modules keep short “when”; tool descriptions keep how-to; ACP/bridges keep real gates — less BINDING prose in prompts
- Citation / experiments / intensive modules slimmed; generic `edit`/`write` on `brief.md` denied (use brief tools)
- Scholarly globals (`research-reasoning` / `reply-depth`) kept; proactive Plan is tool + short capability map (no `plan-consent` module)



### Literature

- `literature-intensive-reading`**:** agent can add/remove/list intensive papers by bibkey, then `literature-read-pdf` (no manual @-toggle required)



### AI Chat — Question tool

- **Cancel** unblocks the question bridge; free-text opens when the model omits options; Send surfaces IPC failures



## 0.5.13 — 2026-07-18



### App Shell & Tabs

- TeX / Library / Experiments open in parallel in RightArea (shortcuts + LeftNav focus without tearing down sibling tabs)
- RightArea and Chat open-tab strips: whole-strip drag-reorder, single insert indicator, drag activates the tab
- Restore Content / RightArea resize mins, edge-drag fringe, empty RightArea placeholder, and Literature empty-library pane
- Terminal mode keeps its tab/PTY when switching away; close shortcut still destroys the session



### Tray & Branding

- Tray shows the open project folder name; menu shortcuts maximize TeX / Literature / Experiments; click toggles the menu
- Splash and console prefixes use **prismnext**



### TeX Workspace Export

- Toolbar **Export**: save compiled PDF, or pack manuscript sources as zip (no `.prismnext/compile`)



### AI Chat

- Soft/Hard turn sliding window for long sessions; **Load earlier messages** when scrolling up
- **Chat Artifact Block** (````artifact`): embed any project result file; PDF first-page peek + viewer dialog; experiment cards share the same shell
- Fix PDF/template dialogs stuck on Loading; fix inline-code crash after artifact/Shiki split
- Prompts prefer artifact fences for experiment results (display ≠ vision)



### Experiments

- Lean `experiment-log` reads with absolute `oldestRun` / `latestRun`; prefer artifacts over stdout tails
- Per-run image `artifactSnapshots`; auto-attach result files when `artifacts[]` is omitted; chat images refresh on mtime



## 0.5.12 — 2026-07-17



### Shortcut Registry & App Hint

- **Shortcut registry** (`src/shared/shortcuts/`): shell/editor fixed, workspace/product remappable-ready; Settings + shell tooltips use resolved chords (`primary` = ⌘/Ctrl)
- Shell wiring: ⌘W cascade (RightArea → chat → close window), ⌘B sidebar, ⌘ RightArea, ⌘, Settings, ⌘S save
- **App Hint**: theme-aware toolbar tooltips (`ui/hint`) with registry shortcut chips; modes / Settings / chat toolbars migrated off native `title=`
- Command palette: single app host + `shell.commandPalette` (⌘K / Ctrl+K); product shortcuts for new chat, chat tabs, compile, accept/reject all
- Editor find panel + keymap wired through the shared shortcut / CodeMirror path



### Chat Open Tabs

- When two or more chat tabs are open, ContentTopBar shows a light open-tabs strip (switch / close / streaming / horizontal scroll)
- Opening a history session discards blank unused New Chat tabs; hover shows session checkout, staged citations, AI terminal
- User-turn bubble insets with **margin** (matches composer width) — no opaque sticky padding plate over glass / capsule surfaces



### First-message Prepare & Task Deny

- First `chat:send` emits `system.prepare` stages (project sync → session/MCP → model) instead of a blank “Thinking…” wait
- Builtin Task deny (`@Explore` / `@general`) shows an explicit tool error + `subAgent.completed`, not opaque `{"error":"Task cancelled"}`



### Chat Stream Integrity

- Fix ghost prior-turn tools/thoughts: ignore late ACP replays of settled `messageId`s / committed `tool_use` ids
- Fix missing / truncated thoughts when `tool_use` precedes `thinking` under the same `messageId` (common on GLM)



### Permission / Literature

- Fix **Auto mode hang**: PTY bash (and delete/move) auto-approve from `tool_call` when OpenCode skips ACP permission
- Chat paper citations binding covers all Paper Search MCP tools; after discovery must `literature-stage`; forbid bash re-parse of `tool-output`



### Settings Logs

- Categories for compile / fs / ipc / crash / security; newest-first list; search includes detail JSON; compile IPC uses structured logger
- Hidden scrollbar utility for open-tabs / TabBar overflow scroll



### Architecture & Testing

- Shared shortcuts + Vitest; chat prepare phases / task-deny message helpers; open-tabs + late-replay + bash-bridge tests
- Specs: shortcut registry, app hint, editor find, chat open tabs



## 0.5.11 — 2026-07-17



### Brand System & App Icons

- Lock **variant A** fork ribbon (P5 Warm Graphite) as the product mark; shared geometry in `brand-mark.ts` + in-app `PrismRibbonMark`
- Ship app icons for packaging: `resources/brand/` (`icon.icns` / `icon.ico` / PNG masters) wired in `electron-builder.yml`
- Welcome mark uses surface-correct plate (`bg-card`); product display name unified to **prismnext** (not “Prism Next”)



### Desktop Notifications & Tray

- Optional **desktop notifications** when a reply finishes or needs attention while the window is in the background
- Optional **tray / menu-bar** icon with idle / busy / attention states; hide-on-close when tray is enabled
- Circular tray mark (solid disk + A ribbon); status differs by ribbon weight only; 16 / @2x 32 template assets



### Profile & Settings

- Pin canonical `userData` to `…/prismnext` with one-time migration from legacy `prism-next` / mistyped spaced folder names
- Settings → Shortcuts: list matches **actual wiring** (Active / UI only / Planned), including workspace ⌘Tab / ⌘L / merge ⌘Y·⌘N



### Architecture & Testing

- Main: `tray`, `desktop-notifications`, `user-data-bootstrap`; shared `desktop-shell` + Vitest
- Specs: brand system, tray circular mark, desktop notifications & tray design/plan



## 0.5.10 — 2026-07-17



### App UI Language (i18n)

- Ship **i18next** UI localization with catalogs for **English**, **简体中文**, and **繁體中文（香港）**
- Settings → General: pick app language explicitly (English first); default **English** — no follow-system option
- Wire translations across shell, Settings (including detail panels), Chat, modes (Git / Literature / Experiments / Files / Terminal / Browser / TeX), Welcome, templates, and dialogs
- Application menu strings follow the selected locale; `zh-HK` generated from `zh-CN` via OpenCC helper script
- Legacy stored `"system"` preference normalizes to English



### Architecture & Testing

- Shared `app-locale` + `menu-i18n`; renderer `LocaleSync` keeps i18n in sync with persisted settings
- Design note: `docs/superpowers/specs/2026-07-17-app-i18n-design.md`
- Vitest coverage for locale normalization



## 0.5.9 — 2026-07-17



### Welcome & New Project

- Redesigned welcome home: brand mark, status line, New/Open, recent projects with icons / Git branch
- New Project dialog: project icon picker, template chips, optional Init Git; Settings **Initialize Git by default**
- Persist `projectIcon` in `.prismnext/settings.json`; show icons on welcome recent list and project switcher



### Chat Attachments & Vision

- Composer / AiBar: drag-drop and attach images plus documents (PDF/DOCX text extract with userData cache)
- Model **vision** capabilities on provider presets; settings UI for vision / helper fallback model
- Non-vision models: `vision-fallback` describes images via helper model so chat can still use screenshots
- Maximized RightArea capsule: model selector sits left of Send (compact + expanded)



### Git Empty States & Composer UX

- Shared **Init Git** control for chat homepage / Git mode empty states; clearer no-repo messaging
- Inline @ / composer dropdown and attach flow polish; panel resize hit targets / cursor stability



### Branding & Packaging

- Product naming unified to **prismnext** in UI, prompts, errors, and User-Agent (keep `.prismnext/` paths)
- `electron-builder.yml`: bundle OpenCode per platform (`darwin` / `windows` / `linux` × arch)
- Dev tool logs: concise `[tool] call|result <name> …` instead of anonymous shape dumps



### Architecture & Testing

- New services: `prompt-file-attachments`, `vision-fallback`; renderer attach/drop helpers
- Vitest coverage for attach image/document, vision fallback, and provider model capabilities



## 0.5.8 — 2026-07-16



### Permission Modes — Auto vs Edit Auto

- **Auto** now means full OpenCode-style auto (edits + shell allow); previous “Auto” (edit auto / shell ask) is **Edit auto** (`edit_auto`)
- Settings migration: stored v1 `"auto"` → `"edit_auto"` via `permissionModeSchemaVersion` v2
- **Allow always**: bash/experiment-run use command-prefix patterns (`bashAllowAlwaysPatterns`); other tools use `toolAllowAlways`
- Permission gate / mode select copy updated; shared `bash-allow-always` + permission timeout helpers



### Experiments Mode — Overview, Runs, Snapshot

- Detail **Overview** surface, runs query helpers, richer runs table / run panel / confirm modal
- `results-snapshot` tool + `experiment-results-snapshot` service for island-wide artifact discovery (no fixed `results/` allowlist)
- Run cancel / full-log / JSONL append hardening; provenance inspector layout fixed (wider dialog, wrap paths, no truncated scroll shell)
- Chat: **discuss** vs **cite-in-paper** for experiment runs; paper-context composer helpers



### Artifact Paths & Chat Images

- Shared `artifact-path` normalization (existence-based + workspace join); `fs:findByBasename` for locatable filenames
- `ChatProjectImage` / markdown image extraction resolve candidates then basename search; `fs:readImage` soft-fails ENOENT
- Append-run normalizes artifact paths on disk so chat embeds stop showing `[Image unavailable]` for manuscript-adjacent files



### LaTeX Compile Discipline

- Prompt: shell TeX engines forbidden; must use `latex-compile` / UI (build under `.prismnext/compile/`)
- Hard gate: ACP + PTY block `pdflatex` / `xelatex` / `lualatex` / `latexmk` / `tectonic` via bash, with deny reason + next-turn redirect



### OpenCode Bundling & About

- Developer download script **pins** `scripts/opencode-version.txt` (currently `v1.17.7`); refuses bare `latest`
- Settings → About shows **OpenCode** bundled binary version alongside prismnext
- Thinking indicator moved to top of assistant reply and hides on first real content



### Architecture & Testing

- New shared modules: `artifact-path`, `latex-compile-bash`, `bash-allow-always`, `opencode-version`, `permission-timeouts`
- Expanded Vitest coverage across permissions, experiments, artifacts, latex gate, and About version parse



## 0.5.7 — 2026-07-15



### Paper Search MCP — Built-in Default & Hardening

- **Curated built-in** `paper-search-mcp` ships as the default MCP server for every project. `project-mcp-defaults.ts` `ensureDefaultMcpServers` seeds, migrates legacy command forms, and force-enables the slot on every project open — first run no longer requires the user to install anything
- **Legacy command hardening**: `isLegacyPaperSearchCommand` rejects `uv`/`uvx`/old `python -m paper_search_mcp`/`node dist/server.js`/app-vendored `.venv` paths, then rewrites to canonical `npx -y paper-search-mcp-nodejs`; `PAPER_SEARCH_MCP_`* env keys are dropped on migration, user keys are preserved
- **Per-session allowlist assist**: `ensureBuiltinMcpInAllowlist` keeps paper-search-mcp in the allowlist even when the user filters down to a narrower set
- New IPC channels `mcp:ensure` / `mcp:apply` (`src/main/ipc/mcp.ts`); `AcpService.applyProjectMcpConfig` reloads live sessions after a config change
- Renderer: `mcp-catalog-panel.tsx` and `tools-mcp-settings.tsx` show a managed-by-Prism row with version + health dot for the built-in; `getPaperSearchMcpHealth` reports npx path
- `mcp-catalog-panel.tsx` and `tools-mcp-settings.tsx` polish for catalog-driven enable/disable



### Provenance Lite — Per-Project Run & Artifact Audit Log

- New per-project append-only event log at `.prismnext/provenance.jsonl` (schema version 1) — three event types: `run_recorded`, `artifact_linked`, `download_recorded`
- `provenance-service.ts`: `recordRunProvenance` (called from `appendRun`), `recordDownloadProvenance`, `resolveRunById`, `resolveRunForArtifact`, `readProvenanceEvents`
- **mtime-inferred artifact binding**: files under the experiment island whose mtime falls in `[startedAt, finishedAt+500ms]` and aren't explicitly listed by the agent are auto-linked with `linkMethod: "mtime_inferred"` (bounded depth 4 / 2000 entries)
- Honest contract: missing provenance shows "may have been copied manually" instead of guessing a nearby run
- New `provenance-query` OpenCode tool — actions `resolve_artifact` / `resolve_run` / `list_recent`
- New IPC: `provenance:getForArtifact` / `provenance:getForRun`; renderer `ExperimentsProvenanceInspector` modal traces an artifact chip back to the run that produced it with link-method tone (explicit = emerald, mtime_inferred = amber)
- `experiment-run` and `append_run` now stamp `chatSessionId` + `provenanceEventId` on the run line in a single write so provenance is reversible



### Experiments Mode — Human Surface (Sprint 0.7)

- New **Experiments** mode in RightArea: `experiments-mode/` ships `Sidebar` + `Toolbar` + `Content` + `open-experiment.ts` deep-link module
- **Browse grid**: card gallery (3-column responsive, sortable by recency); runs/total + relative time on each card
- **Detail**: Overview (id, created, runs, lab path, tags) + Environment (Python, R, Git, venv) side-by-side; bordered Brief strip; unified run console (command + live output + cancel + reuse + terminal); paginated runs table (10/page) with accordion expansion, artifact chips, env summary
- **Deep-link**: agent's `experiment-log open` action triggers `openExperimentInPanel` which force-expands RightArea (split, not maximized) and `selectExperiment`
- **Artifact navigation**: `experiments-artifact-nav.ts` opens artifact chip in Files mode at the lab subfolder
- **Empty states**: no-folder (Settings link), empty list (CTA copy), grid↔detail smooth state machine
- **Module-internal permission confirm modal** (`experiments-run-confirm-modal.tsx`, 15s auto-deny countdown) — D3 decision: doesn't reuse the ACP shell-confirm UI because it's coupled to `requestPermission` lifecycle; main-side backstop still consults `resolvePermissionAction`



### Chat × Experiments Integration

- `ExperimentToolWidget` per-action summaries (`list` / `create` / `read` / `append_run` / `detect_env` / `open` / `run`) with error/hint text; "Open in Experiments" button (only when `experimentId` is resolvable and not loading/error)
- **Inline image gallery**: `ArtifactImageGallery` + `ChatProjectImage` for PNG/JPG/GIF/WEBP/SVG artifacts in run results
- **Natural figure fallback**: `experiment-run-figures.ts` (`renderer/lib/chat/`) extracts figure paths from `run.artifacts` and appends a markdown "本次运行生成的图如下：\n\nname" block to the assistant reply when the model forgets to embed
- `open-experiment.ts` exposes `resolveExperimentIdFromTool` for the widget
- Provenance inspector's "Discuss in chat" / "Open chat session" buttons: `insertExperimentRunToChat` populates the composer, `loadSession` jumps back to the originating chat tab



### Agent Pipeline Hardening

- `kickoffExperimentRun` cleanup: `setImmediate` defers PTY spawn so the IPC handler returns `runId` before early chunks; `runOutputBuffer` in renderer catches the race; `reportResult` writes bridge `resPath` + `experiment:runOutput` + `experiment:runComplete` consistently
- `experiment-log-bridge` unifies `experiment-log` and `experiment-run` under a single dispatcher with shared `processingRequests` de-dup; `provenance-query` rides the same bridge
- `gateExperimentPythonExecution` hardening: `extractCdTargets` regex extraction, `isPythonRelatedCommand` + `isExperimentPythonSetupCommand` + `isExperimentPythonScriptCommand` discrimination, `blockBashPythonScripts` flag for the bash-tool path (executor path is exempt since `experiment-run` is the official entry)
- `tool-permission-registry`: `experiment-log` (`file_write`), `experiment-run` (`shell`), `provenance-query` (`read`), `latex-compile` corrected from `read` → `shell` (it spawns tectonic/latexmk subprocesses — previous mis-classification had silently auto-allowed compile in read-only mode)
- `experiment-run` tool: 11-min tool deadline with graceful PTY kill; timeout returns `experiment_run_timeout` with a hint to read the result via `experiment-log action=read` later
- `experiment-log` tool: 6 actions; `open` validates id then broadcasts `experiment:changed` with `focus: true` to surface the UI to that island
- `app-menus` adds macOS **View** menu entries (reload / DevTools / zoom / fullscreen) for dev convenience
- `.gitignore` excludes `docs/internal/` and `docs/audit/`



### Prompts

- `experiments` orchestrator module (binding): storage split, workflow (brief → list → create → open → run), provenance binding, task handoff to `methodology-auditor` (diagnostic only), human UI binding, research-design boundary
- `core-persona` adds the Python env hard rule (shared workspace `.venv` only, no system pip, no per-island venv)
- `proactive-scheduling` gains the "Experiment execution & logging" capability dimension
- `literature-library` and `chat-citation-staging` updated to align with the new tool surfaces
- `task-delegation`: keep Task for subagent specialists; do not delegate experiment reads/writes/runs via Task



### Layout & UI

- RightArea + RightMainArea: mode-registry-driven, single-active-tab rendering
- `lib/workspace/layout-constants.ts` and `layout-resize-guard.ts`: central layout parameters
- `lib/workspace/right-area-layout.ts` and `right-area-sidebar-layout.ts`: right-pane resize semantics
- `lib/workspace/left-nav/panel-refs.ts`: imperative refs so "open Experiments" can leave TeXWorkspace cleanly
- `expand-settings-detail-panel.ts` honors right-area collapse
- `inline-composer` + `inline-tokens`: experiment-run token guard
- Mode selector + experiment mode entry in left nav
- `ContentTopBar` + `MainToolbar` mode dropdown wiring



### Architecture & Testing

- 117 files changed, 7513 insertions(+), 440 deletions(-)
- New main services: `experiment-log-bridge`, `experiment-ui-events`, `project-mcp-defaults`, `provenance-service`
- New OpenCode tools: `experiment-log`, `experiment-run`, `provenance-query`
- New IPC: `experiment:*`, `mcp:*`, `provenance:*`
- Shared types: `provenance.ts`, `experiment-log.ts` extended with `chatSessionId?` / `provenanceEventId?` / schema-versioned `provenanceEvent`
- New renderer modules under `modes/experiments-mode/` (15 files): content router, grid, detail, run panel, runs table, confirm modal, sidebar, toolbar, empty states, provenance inspector, artifact nav, open-experiment deep-link
- 17 new test files: `bibliography-resolved`, `builtin-mcp-allowlist`, `chat-citation-staging-module`, `experiment-ipc`, `experiment-log-service`, `experiment-python-gate`, `experiment-run-env`, `experiment-venv`, `project-mcp-defaults`, `provenance-bridge`, `provenance-service`, `provenance-types`, `experiment-run-figures`, `experiments-artifact-nav`, `mcp-presets`, `open-experiment`, `right-area-layout`; expanded coverage on `tool-name-infer`, `tool-description`, `context-insert`, `inline-composer`, `experiment-store`, `right-area-sidebar-layout`, `workspace-split`
- Plans/specs under `docs/superpowers/`: `2026-07-11-paper-search-mcp-integration-{plan,design}.md`, `2026-07-15-paper-search-mcp-builtin-hardening-{plan,design}.md`, `2026-07-11-provenance-lite-{plan,design}.md`



## 0.5.6 — 2026-07-09



### Experiments Mode UI (Sprint 0.7 — human surface)

- **Browse grid** — card gallery (3-column responsive) when no experiment is selected; Sidebar collapses for full-width browse
- **Detail overhaul** — Overview + Environment side-by-side, bordered Brief strip, unified **Run console** (command + live output + toolbar), collapsible paginated **History** (10 runs/page, accordion rows)
- **Navigation** — toolbar back to grid; Sidebar auto-opens in detail for quick switch; `clearSelection` + tab `experimentId` sync
- **Copyable metadata** — ID, lab path, env fields, cwd; UI Font vs Code Font tokens (`experiments-detail-chrome.ts`)
- **Empty states** — no folder / empty list copy refined (no broken focus-chat action)



### Live Run Output & Executor

- `experiment:runOutput` IPC stream — PTY chunks forwarded to renderer while UI run is in flight
- `experiment-store` — `runOutputBuffer`, `handleRunOutput`, early-chunk race fix; `clearSelection` action
- **Executor** — `onOutputChunk` callback, `PYTHONUNBUFFERED=1` via `ai-pty` `envExtra`; streaming tail in run panel
- **Shared** — `tailBytes` TextEncoder fallback for renderer; `experimentEnvDisplayRows()` for language-agnostic env rows



### Chat & Agent Discipline

- `experiments` **prompt** — bind experiment commands to `experiment-run` (not raw `bash`); clarify UI/human mode shares same `runs.jsonl`
- **Tool name infer** — distinguish `experiment-run` from `bash` when input has `{ id, command }` (fixes live mislabel)
- **Experiment tool widget** — richer summaries for list/create/read/run/detect_env; expanded empty-state fallback



### App Shell & Testing

- macOS **View** menu — reload, DevTools, zoom, fullscreen (dev-friendly)
- `.gitignore` — `docs/internal/`
- Tests: `experiment-store` (clearSelection, run output buffer), `experiment-log-service` (`experimentEnvDisplayRows`), `tool-name-infer`, `experiment-ipc` runOutput hook



## 0.5.5 — 2026-07-08



### Research Brief (Sprint 0.2 — Idea / Design Platform)

- New **research brief** living doc at `.prismnext/research/brief.md` — nine-section Markdown template (research question, hypotheses, scope, risks, …)
- OpenCode tools `research-brief-read` / `research-brief-update` — section-aware read/patch via file bridge; brief body is **not** pre-injected into system prompt (agent calls tools on demand)
- `research-design` orchestrator module — FINER-oriented workflow, brief discipline, handoff to `research-design-coach` Task
- Settings → Prompts & Rules → **Edit brief** panel (preview/source, ensure-on-open)
- `/brief` slash command — ensure brief exists and kick off brief workflow
- `researchBrief:*` **IPC** for UI; scaffold on `project:ensure`
- Chat **research-brief tool widget**



### Experiment Log (Sprint 0.3 — Analysis Platform)

- **Split storage**: registry at `.prismnext/experiments/<id>/` (`meta.json` + `runs.jsonl`); clean workspace lab at `<experiment-dir>/<id>/` (agent-owned layout)
- OpenCode tools `experiment-log` (list/create/read/append_run/detect_env) and `experiment-run` (env probe → PTY bash → append JSONL in one step)
- `experiments` orchestrator module — workspace Experiment folder required; brief linking at create; methodology-auditor handoff with run snapshots
- `findExperimentConfig` / `resolveExperimentDir` — symmetric with manuscript workspace resolve
- Chat **experiment tool widget** for log/run results
- `methodology-auditor` expert instructions: treat structured run log as factual source



### Expert Roster — Cognitive Specialists

- Replace scout/auditor trio (**citation-auditor**, **library-scout**, **literature-scout**) with five **cognitive experts**: `research-design-coach`, `methodology-auditor`, `literature-synthesizer`, `structure-diagnostician`, `peer-reviewer`
- Orchestrator `research-prism` modules expanded: `research-design`, `experiments`, `proactive-scheduling`, `research-reasoning`, `reply-depth`
- `core-persona` reframed as full **research-loop** agent (not LaTeX-only)
- Experts/skills sync and project refresh hardened (`experts-sync`, `project-experts-refresh`, `project-skills-refresh`)



### Provider & Model Settings

- **OpenCode Go / Zen** preset catalogs aligned with official model IDs (`opencode-go/glm-5.1`, …); shared `opencode-provider.ts` for model ref normalization and legacy ID migration
- `getAllEnabledModels` includes user-added preset providers; custom model **display name** + **context window** in provider editor
- Settings model list shows context badges; dynamic context ring **total** from selected model metadata



### Chat, Markdown & Streaming

- Streaming **block splitter** extended for `$$` / `\[` / `\]` display-math fences; `streaming-code-fence` parser for in-progress code blocks
- KaTeX/math delimiter normalization and integration tests; library cite ref remark updates
- Chat turn scroll and message layout refinements; **project chat prewarm** on open



### Architecture & Testing

- 112 files changed, 5909 insertions(+), 662 deletions(-)
- New tests: `research-brief-service`, `experiment-log-service`, `opencode-provider`, `providers-enabled-models`, `custom-model-form`, block-splitter/math/streaming-code-fence; expanded `resolve-active-modules`, `instructions-audit`, `experts-sync`
- Plans: Sprint 0.2 research brief, Sprint 0.3 experiment log; Phase 0 backlog updated



## 0.5.4 — 2026-07-06



### Update Checker

- New lightweight **update checker** (`update-checker.ts`): fetches a `version.json` manifest from a local path or HTTPS URL, compares semver against the installed version, and caches the result — no `electron-updater`, no auto-download, no code signing required
- New **Settings → About** panel: installed version, update-source input, manual "Check for updates" button with five result states (up-to-date / available / ignored / error / no-source), plus Skip-this-version and Unskip controls
- Manifest fields (`version` / `path` / `releaseNotes` / `pubDate`) mirror electron-builder's `latest.yml`, so a future migration to GitHub Releases + `electron-updater` is a config swap, not a rewrite
- Download opens the manifest's `path` URL via the existing `shell:openExternal` (https-only allowlist) — user installs manually
- Sample `update-channel/version.json` for self-testing the check flow in dev



### Security Hardening

- **Content-Security-Policy** (`csp.ts`): injects a dev/prod-aware CSP via `onHeadersReceived` on the main frame; previously the renderer had no CSP, so any XSS (AI markdown, pdf.js, rehype-raw) had full renderer + filesystem control
- **Active project-root registry** (`active-project-roots.ts`): main-process trust anchor for `fs` IPC path containment — `fs` handlers now check `absPath` against the open project root instead of accepting arbitrary paths; closes a hole where a compromised renderer could read/write/delete any file
- `crash-handler.ts` registers `uncaughtException` / `unhandledRejection` handlers that push to the structured logger and synchronously append to a durable `crashes.log` (the async logger may not flush before a hard crash)



### ACP & Event System Stability

- **Task watchdog** in the event mapper: detects stalled orchestrator Task dispatches and surfaces clear errors instead of hanging subagents
- `messageId` on `message.part.updated` and per-turn `clearTurnAccumulators` reset harden the streaming event path
- **Chat cancel preserves** completed turns: cancelling mid-stream no longer discards already-finished assistant content
- Tool-name inference and tool-permission-registry tests expand coverage for ACP tool gating



### Toolchain Cleanup

- `pnpm typecheck` / `typecheck:renderer` / `typecheck:main` scripts; tsconfig include paths tidied
- `literature-cite-check` → `citation-health` rename; `literature-cite` → `literature-delete`; removed `latex-bib-check` (folded into the citation-health flow)
- `opencode-plugin.d.ts` and `citation-js.d.ts` type declarations for third-party modules
- `.gitignore` excludes `docs/audit/`, `.pnpm-store/`, `.vscode/`



### Architecture & Testing

- 108 files changed, 3098 insertions(+), 582 deletions(-)
- New tests: crash-handler, csp, active-project-roots, event-mapper task watchdog, tool-permission-registry, tool-name-infer, chat-cancel-preserve



## 0.5.3 — 2026-07-05



### LaTeX Agent Tools

- New `latex-root` / `latex-compile` / `latex-bib-check` tools for agent-driven LaTeX root resolution, compilation with structured errors, and `.tex` ↔ `.bib` key audit
- New `literature-cite-check` / `literature-export-bib` tools for `.tex` ↔ library.db citation compliance and library-to-`.bib` sync
- Dedicated **LaTeX tool widgets** in chat for compile result summaries, engine/bib tool info, and missing/unused/duplicate key reports



### Compilation Pipeline Rewrite

- **Build-dir architecture**: manuscript sources synced to `.prismnext/compile/` before compilation — `.tex`, `.bib`, figures, and class files colocated for correct LaTeX path resolution
- **Bibliography staging**: declared `.bib` files copied into the build dir, fixing biber/bibtex resolution when aux/bcf files live in a separate output directory
- **Intelligent multi-pass compilation**: auto-detects unresolved bibliography and runs extra bibtex/biber + LaTeX passes; terminates once cross-references and bibliography entries converge
- **Undefined citation detection**: compile reports `"Citations unresolved"` when `\cite{...}` keys missing from `.bib` instead of a generic failure message
- **SyncTeX path mapping**: build-dir output paths mapped back to project source tree



### Bibliography Resolution & LaTeX Root

- `bib-path-resolve.ts`: resolve `\addbibresource` / `\bibliography` paths relative to main `.tex`; construct `BIBINPUTS` for bibtex/biber
- `latex-root.ts`: resolve main `.tex` with 6-level fallback (workspace config → `% !TEX root` → documentclass → `main.tex` → first `.tex`); walk project recursively



### Citation Health System

- Unified `.tex` **↔** `.bib` **↔ library.db** audit with `getCitationHealth`; import missing keys from `.bib` into library, or sync library entries into manuscript `.bib`
- **Citation Health dialog** in Literature mode with status rows, importable-entry listing, and import/sync action buttons
- **Session citation audit context**: tool results persisted per chat session as JSON snapshots; injected as `## Session citation audit (this chat)` turn appendix — agents reuse cached results rather than re-scanning



### Task Orchestrator Gate

- `task-orchestrator-gate` blocks orchestrator from delegating builtin platform tools (`latex-bib-check`, `literature-cite-check`, etc.) via Task; enforced at both event-mapper and permission layers
- Denied tasks emit clear error instructing the orchestrator to call tools directly in the conversation



### Prompt Modules

- New **citation-audit** and **latex-workspace** binding rule modules; updated **literature-library** (library-first `.bib` policy) and **task-delegation** (no Task for platform tools)



### IPC & Event System

- New IPC handlers: `literature:citationHealth`, `literature:mergeIntoProjectBib`, `literature:importFromProjectBib`
- New `compile:agentComplete` broadcast event with preload bridge for agent-driven PDF preview updates
- Event mapper stability: `messageId` on `message.part.updated`, `clearTurnAccumulators` per-turn reset



### Literature & MinerU

- `mergeLibraryIntoProjectBib` syncs library papers to manuscript `.bib`; `findProjectBibPath` respects workspace config
- MinerU blocks: IoU-based content_list matching replaces sequential index pairing; `blocksNeedLayoutUpgrade` heuristic
- PDF selection rect extraction for precise block quoting



### LaTeX Language Support

- Custom `prism-latex-language.ts` with syntax highlighting, command completion, environment folding, and citation key support in CodeMirror



### Architecture & Testing

- Design/plan docs under `docs/superpowers/` for latex agent tools and platform capabilities
- 21 new test files covering all new services and tools; expanded coverage on existing tests
- 101 files changed, 6074 insertions(+), 350 deletions(-)



## 0.5.2 — 2026-07-04



### Expert Team (OpenCode Subagents)

- Replace **Agent mode profiles** with **Orchestrators** + **Experts**: bundled Research Prism orchestrator and citation / literature / library scout experts; custom experts and orchestrators with permission presets
- Experts sync to app-level OpenCode agents under `userData/opencode-server/`; orchestrator delegates to subagents via OpenCode **Task**; **Task** tool widget in chat with subagent dispatch UI
- Composer **@Expert** mentions (multi-select in Expert team mode); `@` dropdown section labeled **Experts**; per-tab orchestrator selection removed from composer toolbar
- Settings → **Agent**: orchestrator/expert CRUD, built-in enable toggles, reset-to-defaults; one-shot migration from legacy `profiles-manifest.json` / `profiles/custom/`
- Disabled built-in experts stay visible in the list (`enabled: false`) instead of disappearing; optimistic Switch toggles without full-list Loading flicker



### Prompt Stack & Modules

- Four-layer prompt model: `_prism-system`, `AGENTS.md`, project rules (per-turn), orchestrator `agent.md`; **Preview stack** settings panel with expandable per-layer cards (replaces flat assembled markdown)
- Per-turn injection for intensive-reading context; new **task-delegation** module for orchestrator Task guidance
- Retire profile overlay and legacy domain modules (academic-writing, citations, figures-tables, math-equations); literature-library and chat-citation-staging modules updated for expert-team flow



### Skill Library & Install

- Unified **Skill Library** settings panel: Prism Curated catalog, remote registries, GitHub `owner/repo` sources, connect/disconnect, install single or all packages
- Install pipeline: GitHub tree scan, publisher registry discovery, archive extract (`tar` / `extract-zip`), content digest, update checks, and reinstall from recorded origin
- Skill enable/disable uses lighter OpenCode config sync (no full reload storm); manifest-only filesystem changes skip redundant scheduled refresh; Skills settings silent background reload on integration events



### Chat Citations & Library Context

- Clickable **library paper citations** in chat markdown (`remark-library-cite-refs`); inline citation chips open papers in Literature mode
- Session citations context injected into orchestrator/expert prompts; **expert-team preamble** for multi-agent turns; library-task context for project library handoffs
- Assistant message block-list rendering refactor; static markdown and citation-staging sync hardened for library cite markers



### Settings & UX

- Switch component: `duration-200 ease-in-out` thumb/track animation; optimistic toggles for MCP servers, built-in slash commands, agent experts, and project skills
- Built-in slash commands panel; knowledge modules panel; shared settings JSON toolbar; commands/MCP/skills settings layout polish



### Architecture & Testing

- Remove profiles IPC/sync; add experts IPC, `experts-sync`, bundled experts/orchestrators, `project-experts-refresh`, `agent-editor-options`
- Prism bridge path helpers shared across custom OpenCode tools; literature tools and bash permission bridge aligned with expert runtime filters
- Design/plan docs for expert team, prompt stack, and skill library under `docs/superpowers/`
- Vitest expansion: experts-sync, skill-install (GitHub/digest/updates), prompt-stack-preview, task-delegation, session-citations, library-cite refs, compile-composer experts, and related renderer smoke tests



## 0.5.1 — 2026-07-02



### PDF Extraction & Agent Reading

- **PDF body extraction** pipeline: MinerU (cloud), pdf.js, and HTML fallbacks; queue with retry, progress IPC, and `.prismnext/library/extract/` storage
- OpenCode tool `literature-read-pdf` — read extracted Markdown by bibkey with `pages=` / `query=` / `force=`; gated to **intensive reading list** in prompts
- Reader **block overlay**: hover/select PDF blocks (MinerU layout), toolbar actions, colored highlights layer, excerpt → chat / notes
- `paper-extract` shared types, slice utilities, and extract store; Settings → Literature controls for default engine and auto-extract on import



### Chat Citations & Prompt Architecture

- `literature-stage` workflow hardened: session-scoped staging, `[n]` citation refs, **Session citations** panel with staged metadata and **Add to library** (including progress phases)
- **Prism Tools Guide** dynamic prompt module (`prism-tools`) + `prompt-sync` writes system instructions to OpenCode; tool registry as single source for agent-facing descriptions
- `chat-citation-staging` and `literature-intensive` modules; citations module trimmed to pure LaTeX/BibTeX knowledge
- Agent Tools panel in Settings (read-only view of registered OpenCode tool descriptions)
- Faster staging catalog path: arXiv-first resolve, unified `mainNetFetch` (Electron `net.fetch`, system proxy) for Crossref / arXiv / OpenAlex; OpenAlex arXiv lookup via `10.48550/arxiv.*` DataCite DOI



### Reader Sidebar & Citation Network

- Git-style sidebar tabs: **Notes | Marks | Refs | Cited by**; paper header chrome simplified
- **Citation network** via OpenAlex with Semantic Scholar fallback; paginated Refs / Cited-by lists, disk cache under `library/cache/citations/`, in-library vs **Open online** actions
- Shared `openalex-lookup` URL builder; improved fetch error messages (Chinese) for network and rate-limit cases



### Library UX & Metadata

- **AI metadata** background queue (title/tags/summary heuristics + provider chat); `literature-ai-metadata` settings and entry-panel badges
- **Canonical paper tags**: JSON storage, FTS indexing, tag filter dropdown, suggest input, agent-readable tag search on `literature-search`
- PDF **attach / drag-drop / identity check** (DOI·arXiv·SHA); add-by-identifier dialog; batch selection actions; library subview prefs; marquee multi-select in list
- **Intensive reading list** store + composer toolbar button; sync with chat session registry
- Zotero **materialize** improvements — linked papers survive when PDF or extract lands locally; orphan shell cleanup on disconnect



### Database & Reliability

- FTS schema migration fix: rebuild `papers_fts` when `tags` / `ai_summary` columns missing (fixes **Add to library** after upgrade)
- Bibliographic resolver: catalog `catalogFetch` injection, clearer staging failure diagnostics, arXiv retry on timeout/rate-limit



### Architecture & Testing

- Design/plan docs for PDF agent reading, AI metadata/tags, and citation network under `docs/superpowers/`
- `main-network.ts` as canonical main-process HTTP route; pdfjs Node polyfills for main extract path
- Vitest expansion: extract/MinerU, citation network, FTS migration, AI metadata, tags, staging, Zotero materialize, openalex-lookup, and renderer literature UX helpers



## 0.5.0 — 2026-07-01



### Literature Library & Reader

- **Literature mode** in RightArea: project library (`.prismnext/library/library.db`), PDF reader (Lector + pdf.js), collections sidebar, entry panel with CSL metadata, inline field editing, and reading-notes pane
- Import PDF / BibTeX (+ optional Better BibTeX `.json`), add by DOI or arXiv ID, manual entry, duplicate detection, FTS search, and orphan-attachment cleanup
- **Enrich pipeline** resolves metadata from Crossref, arXiv, OpenAlex, Semantic Scholar, DataCite, DBLP, and OpenReview; auto-attaches PDFs when catalogs expose them
- PDF cache under `.prismnext/library/attachments/`; custom `prism-literature-pdf://` protocol for in-app rendering; SyncTeX-free read-only preview path shared with existing PDF toolbar patterns
- **Workspace split** for library list + reader/notes; toolbar subview dropdown (**All entries** / **Session citations**); left sidebar **Library** entry opens maximized RightArea (same pattern as TeX Workspace)



### Zotero Integration

- Optional Zotero desktop + Web API: probe status, Better BibTeX detection, collection bind/sync, streaming PDF fetch into local attachments (no full Zotero data dir import)
- Connect dialog, disconnect, push/pull writers, and CSL-JSON helpers; Settings → **Literature** panel (storage stats + Zotero credentials — replaces standalone Zotero settings page)



### Agent Tools & Chat Citations

- OpenCode tools: `literature-search`, `literature-read`, `literature-cite`, `literature-stage` (verify-only, default for citing), `literature-add` (explicit library write)
- Main-process **literature bridge** for tool IPC; prompt rules in `citations.ts` — stage first, cite as `[n]`, no auto-add to library
- **Session citation staging**: per-chat-session store (localStorage), **Session citations** panel, staged entry panel, **Add to library** / **Add all**, panel-only **Clear** (chat `[n]` links preserved)
- Clickable `[n]` in chat markdown (`remark-citation-refs`); jump to Literature session list with RightArea expand when collapsed
- `@paper` composer mentions inject bibliographic context + reading notes; `paper-snippet` and literature tool widgets in chat



### Files, Notes & Workspace

- Default `notes/` workspace folder; per-paper note directories (`notes/{bibkey}/`), frontmatter-linked **Open in Literature**, PDF selection → insert quote into note
- Configurable workspace folder icons; literature note tabs and recover-paper-from-note helpers
- Files sidebar notebook badge; open-project-file routes literature PDFs and notes into the right modes



### Bibliography & Metadata

- Shared `bibliographic-metadata` resolver with rate-limit-aware catalog fetch and normalization helpers
- Bibliography IPC (BibTeX export), bibkey utilities, DOI/arXiv normalization, BibTeX parse/import
- CSL bibliography tests and paper identity / cite-check coverage



### Layout & Settings

- Literature registered in mode registry; tab lifecycle for literature paper tabs; git-mode row chrome tweaks alongside literature sidebar integration
- Settings sidebar restores **Literature** under Components; TeX workspace settings cross-link literature/Zotero behavior



### Architecture & Testing

- Plans/specs under `docs/superpowers/` for reader, caching, Zotero decoupling, citation staging, and global library design
- `ensure-native-modules.mjs` dev bootstrap; tool-name inference for ACP event mapping
- Broad Vitest suite: literature service/bridge/enrich/PDF cache, Zotero client/sync, citation staging, remark citation refs, jump-to-citation, paper notes, workspace split

