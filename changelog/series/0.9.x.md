# Changelog — 0.9.x series

> **Detailed dev log** for this line. GitHub Release uses the summarized copy in [`../releases/`](../releases/) (regenerate with `pnpm release:changelog 0.9.0` before tagging).

## 0.9.0 (Unreleased)

### Remote

- Remote hosts are read from this computer’s `~/.ssh/config` (including `Include`). You do not fill host / user / key in Settings. Open the Workbench add-project menu to see the list and connect.
- Connection uses the system `ssh` / `scp` already on your PATH, so ProxyJump, ssh-agent, and your existing keys work the same as in a terminal. A first-time host key is confirmed in the connection panel, then written to `~/.ssh/known_hosts`.
- The first successful connect still pushes this app’s Host runtime from the notebook (stamp = app version + checksum). A missing local payload fails locally; the server is not asked to download Host.
- Remote Workspace stays behind the Pro entitlement `workspace.remote`. Without a license the host list shows a short upgrade note and will not connect.
- Chat’s Local / worktree control is unchanged — it is still the git checkout switch, not the remote-machine switch.
- First connect after SSH succeeds no longer dies with “could not resolve remote $HOME”: the system `ssh` command is sent as one script, so `$HOME` is expanded on the server.
- Connect now keeps a constitution: each gate (license, payload, SSH, host key, home, push, Node, host process, handshake, doctor) is logged and shown in a dedicated connection panel, not inline in the Workbench add menu.
- Connecting no longer requires Node on the server PATH. The app pushes a dedicated Linux Node 24 LTS inside the Host payload (same idea as VS Code / Cursor Server) and starts `prismnext-host` with that binary.
- After a host is connected, the Workbench add menu opens a second-level list for that machine: recent remote projects, Open Folder, and New Project. Connecting does not open `$HOME` as a project; you pick a folder first. The file tree and editor talk to that folder over SSH; Chat send is blocked until Remote Agent (RW-2). The sidebar marks those projects as Remote.

### Developer

- New `src/shared/remote` contracts (`ProjectHandle`, SSH frames, Host handshake, `SshConfigHost`) and a `prismnext-host` control-plane binary (`doctor`, `serve --stdio`) packed with `pnpm host:pack` into `out/host/` / extraResources. Transport default is system OpenSSH; the unused `ssh2` / `@types/ssh2` packages were removed.
- `pnpm host:pack` now embeds official Node 24 LTS (`24.19.0`) `bin/node` for `linux-x64` and `linux-arm64` (cached under `scripts/host/.node-cache/`). Payload filenames are `prismnext-host-linux-*.tar.gz`; connect picks by remote `uname -m`, not the laptop arch.
