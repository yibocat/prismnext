node_modules/
.pnpm-store/
out/
dist/
.DS_Store
*.log
*.tsbuildinfo
__pycache__/
*.pyc
src/renderer/public/pdfjs-dist/

# Leftover Host pack caches (no longer used; pack does not download binaries)
scripts/host/.node-cache/
scripts/host/.tectonic-cache/
scripts/host/.git-cache/

# Tectonic binaries (downloaded or manually placed)
bin/tectonic/
# Leftover Typst CLI downloads — unused after Tinymist-only; keep ignored if present
bin/typst/
# Tinymist binaries (downloaded or manually placed)
bin/tinymist/
# Leftover local OpenCode agent binary — unused; do not commit if still present
bin/opencode/
.cursor/

# Copilot local instructions (mirror of .cursor/rules/ — machine-local, not committed)
.github/instructions/
scripts/sync-cursor-rules.mjs

# Pro packs staging dir — build artifact of scripts/prepare-pro-packs.mjs (private content, never commit)
resources/pro-package/

# Private / internal docs (specs, plans, audits) — not for open source
docs-private/

.pnpm-store/
.vscode/

# Brainstorm mockups
.superpowers/
.claude/

# Accidental local mkdir of a remote:// URI (Node path.resolve)
/remote:
