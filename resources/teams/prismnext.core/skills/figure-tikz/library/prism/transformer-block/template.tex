\documentclass[border=6pt]{standalone}

\usepackage{tikz}
\usetikzlibrary{positioning, arrows.meta}

\definecolor{otblue}{HTML}{0072B2}
\definecolor{otorange}{HTML}{E69F00}
\definecolor{otteal}{HTML}{009E73}
\definecolor{otpurple}{HTML}{CC79A7}
\definecolor{otgray}{HTML}{5A5A5A}

\begin{document}
% ==== parameters (edit these) ============================================
\def\boxw{3.4}
\def\boxh{0.85}
\def\gap{0.55}
\def\inputlabel{Input $x$}
\def\mhalabel{Multi-head attention}
\def\normonelabel{Add \& Norm}
\def\ffnlabel{Feed-forward}
\def\normtwolabel{Add \& Norm}
\def\outputlabel{Output}
\def\qlabel{$Q$}
\def\klabel{$K$}
\def\vlabel{$V$}
% =========================================================================

\begin{tikzpicture}[
  >={Stealth[length=2.4mm]},
  node distance=\gap cm,
  blk/.style={
    draw=otblue!75!black, fill=otblue!16, rounded corners=2pt,
    minimum width=\boxw cm, minimum height=\boxh cm,
    align=center, font=\sffamily\small,
  },
  io/.style={
    draw=otgray!60, fill=otgray!12, rounded corners=2pt,
    minimum width=\boxw cm, minimum height=\boxh cm,
    align=center, font=\sffamily\small,
  },
  qkv/.style={
    draw=otorange!80!black, fill=otorange!18, rounded corners=2pt,
    minimum width=0.85cm, minimum height=0.55cm,
    font=\sffamily\footnotesize,
  },
  flow/.style={draw=otgray!70, line width=0.9pt, ->},
  skip/.style={draw=otteal!80!black, line width=0.8pt, ->, dashed},
]
  \node[io] (input) {\inputlabel};
  \node[qkv, below left=0.35cm and 0.15cm of input] (q) {\qlabel};
  \node[qkv, below=0.35cm of input] (k) {\klabel};
  \node[qkv, below right=0.35cm and 0.15cm of input] (v) {\vlabel};
  \node[blk, below=1.15cm of input] (mha) {\mhalabel};
  \node[blk, below=of mha] (n1) {\normonelabel};
  \node[blk, below=of n1] (ffn) {\ffnlabel};
  \node[blk, below=of ffn] (n2) {\normtwolabel};
  \node[io, below=of n2] (output) {\outputlabel};

  \draw[flow] (input) -- (k);
  \draw[flow] (q) -- (mha);
  \draw[flow] (k) -- (mha);
  \draw[flow] (v) -- (mha);
  \draw[flow] (mha) -- (n1);
  \draw[flow] (n1) -- (ffn);
  \draw[flow] (ffn) -- (n2);
  \draw[flow] (n2) -- (output);
  \draw[skip] (input.west) -- ++(-0.85,0) |- (n1.west);
  \draw[skip] (n1.east) -- ++(0.85,0) |- (n2.east);
\end{tikzpicture}
\end{document}
