# 2026-07-04-pending-issues

- [x] 1. 主要 Latex 的编译问题仍然断裂，且被考虑复杂了。 manuscript/ 作为主要写作目录，而 .prismnext/compile/ 作为编译产物的目录，文件和编译产物放在两个问题。关键是 bib 文件放在 manuscrip/ 下无法被编译文件夹 .prismnext/compile/ 获取到，所以一直报错一直有错。是在不行就直接取消 .prismnext/compile/ 了可以不？直接在 manuscript/ 编译就可以了。
- [x] 2. literature 模式阅读文章，对于打开的 PDF ，任何操作只要离开了 PDF ，阅读的位置就会中断，比如切换，切换标签，New Agent（这也是个问题，New Agent 不能影响 RightArea 的啊，不能关闭 Rightarea 的啊），新建笔记等等。意思是，只要能影响到阅读的，都会造成 PDF 重刷新，从而从头开始阅读。
- [x] 3. 我和 AI 通过几次交互之后，以前的老问题又出现了，旧的交互对话中 AI 调用的工具和thought 仍然会出现在最后一次对话交互的末尾。看起来非常难受。
- [x] 4. literature 的 session citations，居然和 AI 的回复中对不上！我点击的哪篇参考，右侧打开的确实另一篇的 citation，这个非常重要，参考文献怎么能引用错了呢？？？ 这是一个 bug 还是说因为模型的能力不佳导致的？
- [x] 5. literature 中阅读 PDF，PDF 内部的交叉引用不起作用。
- [x] 6. PDF 中的参考文献解析不起作用有时候，右侧侧边栏中无法显示该文章的参考文献。这是因为接口中获取到的本来就没有吗？
- [x] 7. 阅读文献的时候，右键一个框应该出现对于该框的操作，而不是右键则选中鼠标位置的一个单词。目前仅有 shift+click 才起作用，这样单靠键盘的体验不佳。
- [x] 8. 当没有 文章的 Note 的时候，选中一块添加到 Note，应该是创建笔记并添加进去，而不是创建笔记但是不添加，这个没有处理好吧
- [x] 9. 跨行选中 PDF 的文字，然后添加 Lector 的 highlight，应该按照我们所选的文字进行 highlight，而不是选中的行。这个问题就好像是，我明明从第一行的某处开始划选，然后划选到第二行的某处非最右端，结果，添加 highlight 会直接把1,2行添加了，这不合适。
- [x] 10. 每次 AI 回复完了，为什么会跳转到该次交互的信息顶部？而不是最底部呢？这个体验也有些不好啊